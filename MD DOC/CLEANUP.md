# Cleanup inventory

Paths are relative to the repository root. Current datasets, selected runs,
`.venv` and `Previous/` are preserved. All original root Markdown files were
copied to ignored `local/document_history/*.txt` before consolidation.

## Documentation consolidation

Root `CONVERSION.md`, `DATASET.md`, `DATA_COLLECTION.md`, `DELIVERY.md`,
`DEPLOYMENT_REPORT.md`, `ENVIRONMENT.md`, `IMPLEMENTATION.md`, `TRAINING.md`,
`TRAINING_STABILITY.md`, `VIDEO.md` were replaced by permanent guides here.
The README was rewritten; its original is also archived.

## Removed after ONNX, full-video, API and browser acceptance

- `artifacts/phase1`: downloaded wheel/COCO checkpoint and initial smoke outputs.
- `artifacts/phase2` through `artifacts/phase5`: old generated outputs.
- `artifacts/deployment`: obsolete conversion candidates and generated video.
- `artifacts/engine_backups`: obsolete engines after versioned runtime acceptance.
- `artifacts/api_smoke_ada7c6ceac454afcbb940c14363e34c1`: previous generated job.
- `models/goggles.engine`, `models/goggles.toml`, `models/footwear.engine`,
  `models/footwear.toml`: replaced by verified versioned assets/local registry.
- `data/sample_media`: video preserved with matching SHA256 in `local/media`.
- `data/sample_goggles`: all 95 files matched SHA256 against preserved copies
  in `Previous/PPE Googles.coco` before deletion.

Exact resolved paths/counts/bytes were saved in `local/cleanup_inventory.json`
before deletion; each entry now records `removed`. Reparse points were rejected.
All training runs (including failed runs), current datasets, selected checkpoints,
saved configurations, evaluation reports, `.venv`, `Previous`, final models,
current verification outputs, clean-install environment and application jobs
remain local-only. Caches are ignored and may be regenerated by the live server.
Removed outputs are regenerable; verified release copies remain.
